#include <iostream>

using namespace std;

int main()
{
    long long int m, n, a, b, c, a1, a2, a3, b1, b2, b3, c1, c2, c3;
    cin >> m >> n;
    while(n--)
    {
        cin >> a;

    }

    return 0;
}
